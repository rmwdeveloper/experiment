import React from 'react';
import withStyles from 'isomorphic-style-loader/lib/withStyles';
import styles from './StockListingBlock.css'; //eslint-disable-line
import cx from 'classnames';

function StockListingBlock() {
  return (
      <div>
        
      </div>
  );
}

export default withStyles(styles)(StockListingBlock);
