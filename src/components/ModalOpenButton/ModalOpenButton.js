import React from 'react';
import withStyles from 'isomorphic-style-loader/lib/withStyles';
import styles from './ModalOpenButton.css'; //eslint-disable-line


function ModalOpenButton() {
  return (
   <div>Modal Open Button</div>
  );
}

export default withStyles(styles)(ModalOpenButton);
