import {

} from '../constants';

const initialState = {
  projects: [
    
  ]
};

export default function projects(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
